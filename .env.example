# Supabase Configuration
# Get these values from your Supabase project settings > API
# IMPORTANT: Never commit the .env file with real values to git!

# Supabase Project URL (found in Settings > API > Project URL)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon/Public Key (found in Settings > API > anon public)
# This key is safe to use in the browser - it respects RLS policies
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# NOTE: Never put the service_role key in frontend code!
# Service role key should only be used in Edge Functions or server environments
